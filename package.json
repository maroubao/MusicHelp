{
  "name": "music-help",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Actions based monthly listening automation scaffold.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check-config": "tsx src/cli/check-config.ts",
    "session:inspect": "tsx src/cli/session-inspect.ts",
    "task:monthly": "tsx src/index.ts"
  },
  "dependencies": {
    "yaml": "^2.8.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
